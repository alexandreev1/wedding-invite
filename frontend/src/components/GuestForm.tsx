import { memo } from 'react';

function GuestForm() {
    return (
        <div>
            <span>Выберите подходящий вариант напитков, который вы предпочитаете на фуршете:</span>
        </div>
    );
}

export default memo(GuestForm);
